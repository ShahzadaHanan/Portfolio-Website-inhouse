name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SHAHZADAHANAN }}'
          channelId: live
          projectId: ${{ vars.FIREBASE_PROJECT_ID }}



# # Deploys the repo to the LIVE channel of Firebase Hosting
# # whenever a commit lands on main.
# #
# # Place this file at: .github/workflows/firebase-hosting-merge.yml

# name: Deploy to Firebase Hosting on merge

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: Install dependencies
#         run: npm ci

#       # ---------------------------------------------------------------
#       # If your project needs a build step (React, Vue, Angular, Next.js
#       # static export, etc.), uncomment the two lines below and make sure
#       # the "public" path in firebase.json points at the build output
#       # folder (e.g. "dist" or "build").
#       #
#       # If your site is plain HTML/CSS/JS with no build step, leave this
#       # commented out - the checked-out files will be deployed as-is.
#       # ---------------------------------------------------------------
#       # - name: Build project
#       #   run: npm run build

#       - name: Deploy to Firebase Hosting
#         uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: '${{ secrets.GITHUB_TOKEN }}'
#           firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
#           channelId: live
#           projectId: ${{ vars.FIREBASE_PROJECT_ID }}
